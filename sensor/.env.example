# Backend API Configuration
BACKEND_URL=https://yourApp-production.up.railway.app

# Bin Identification (change this for each device)
BIN_ID=bin-<bin_number>

# Post Interval (seconds between backend posts, default: 30)
POST_INTERVAL=30
